# Public Assets

Place your logo image file here as `logo.svg` (or `logo.png`).

The logo should be:
- Named: `logo.svg` (or `logo.png`)
- A vibrant lime green square with a black recycling symbol
- Used in: Navbar, Footer, and as the favicon

